<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RLM_Sync_Quote_Before_Closed_Won</fullName>
    <active>true</active>
    <description>Blocks moving an Opportunity to Closed Won until a quote is synced (SyncedQuoteId populated). The synced quote is the source for the automated Create-Order-from-Quote prompt on the Opportunity record page.</description>
    <errorConditionFormula>AND(
    ISPICKVAL(StageName, "Closed Won"),
    ISBLANK(SyncedQuoteId)
)</errorConditionFormula>
    <errorMessage>Sync a quote to this opportunity before marking it Closed Won. Open the quote, use Start Sync, then close the opportunity.</errorMessage>
</ValidationRule>
