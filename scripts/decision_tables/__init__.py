"""Standalone BRE Decision Table inspection and lifecycle tools.

Authentication is delegated to the ``sf`` CLI. Definitions use Metadata or
Tooling API; CSV data uses Connect; source rows and recipe mappings use REST.
Mutators preview by default and require ``--confirm`` to write.

This package is independent of CumulusCI tasks. See ``README.md`` for commands
and ``.cursor/skills/decision-tables/SKILL.md`` for workflow guidance.
"""
